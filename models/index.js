// Index to establish rules for models Comment, Post and User
const User = require("./User");
const Post = require("./Post");
const Comment = require("./Comment");

module.exports = { User, Post, Comment };
